# Contract Doc Structure

Each per-contract file should cover:

- Class name and source file
- Config schema with a YAML example
- Termination behaviour
- Example use cases with YAML snippets

Keep these focused on how to use the contract in a graph, not on implementation internals.
